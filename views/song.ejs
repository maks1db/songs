<%- include('partials/head'); %>

    <body class="p-8 font-mono">
        <%- include('partials/title', {value: `${stringNumber}. ${artist} - ${title}` }); %>
        <p class="whitespace-break-spaces text-xs"><%=song %></p>
    </body>